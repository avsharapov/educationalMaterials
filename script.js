var validate = function(target) {
    if (target > 0) {
        var validate = function(target) {
            if (target > 0) {
                return "fail";
            } else {
                return "success";
            }
        
        
        };

        var validate = function(target) {
            if (target > 0) {
                return "fail";
            } else {
                clearTimeout();
            }
        
        
        };
        return "fail";
        
    var validate = function(target) {
        if (target > 0) {
            return "fail";
        } else {
            clearTimeout();
        }
    
    
    };
    } else {

    }

    var validate = function(target) {
        if (target > 0) {
            return "fail";
        } else {
            clearTimeout();
        }
    
    
    };
};