var validate = function(target) {
    if (target > 0) {
        var validate = function(target) {
            if (target > 0) {
                return "fail";
            } else {
                return "success";
            }
        
            if (target > 0) {
                return "fail";
            } else {
                return "success";
            }
        };
        if (target > 0) {
            return "fail";
        } else {
            return "success";
        }
        return "fail";
        if (target > 0) {
            return "fail";
        } else {
            return "success";
        }
    } else {
        var validate = function(target) {
            if (target > 0) {
                return "fail";
            } else {
                clearTimeout();
            }
        
        
        };

        if (target > 0) {
            return "fail";
        } else {
            return "success";
        }        return "success";
        if (target > 0) {
            return "fail";
        } else {
            return "success";
        }
    }

    if (target > 0) {
        return "fail";
    } else {
        return "success";
    }
};